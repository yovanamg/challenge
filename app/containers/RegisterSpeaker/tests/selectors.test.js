// import { fromJS } from 'immutable';
// import { makeSelectRegisterSpeakerDomain } from '../selectors';

// const selector = makeSelectRegisterSpeakerDomain();

describe('makeSelectRegisterSpeakerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
