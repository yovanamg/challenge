// import { fromJS } from 'immutable';
// import { makeSelectEventsDomain } from '../selectors';

// const selector = makeSelectEventsDomain();

describe('makeSelectEventsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
