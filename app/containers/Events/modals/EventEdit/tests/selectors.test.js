// import { fromJS } from 'immutable';
// import { makeSelectEventEditDomain } from '../selectors';

// const selector = makeSelectEventEditDomain();

describe('makeSelectEventEditDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
