// import { fromJS } from 'immutable';
// import { makeSelectEventNewDomain } from '../selectors';

// const selector = makeSelectEventNewDomain();

describe('makeSelectEventNewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
