// import { fromJS } from 'immutable';
// import { makeSelectEventDeleteDomain } from '../selectors';

// const selector = makeSelectEventDeleteDomain();

describe('makeSelectEventDeleteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
