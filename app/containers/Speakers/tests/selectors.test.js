// import { fromJS } from 'immutable';
// import { makeSelectSpeakersDomain } from '../selectors';

// const selector = makeSelectSpeakersDomain();

describe('makeSelectSpeakersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
