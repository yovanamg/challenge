// import { fromJS } from 'immutable';
// import { makeSelectUsersDomain } from '../selectors';

// const selector = makeSelectUsersDomain();

describe('makeSelectUsersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
