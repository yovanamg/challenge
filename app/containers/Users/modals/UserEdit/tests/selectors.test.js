// import { fromJS } from 'immutable';
// import { makeSelectUserEditReducerDomain } from '../selectors';

// const selector = makeSelectUserEditReducerDomain();

describe('makeSelectUserEditReducerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
