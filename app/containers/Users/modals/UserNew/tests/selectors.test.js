// import { fromJS } from 'immutable';
// import { makeSelectUserNewReducerDomain } from '../selectors';

// const selector = makeSelectUserNewReducerDomain();

describe('makeSelectUserNewReducerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
