// import { fromJS } from 'immutable';
// import { makeSelectUserDeleteDomain } from '../selectors';

// const selector = makeSelectUserDeleteDomain();

describe('makeSelectUserDeleteDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
